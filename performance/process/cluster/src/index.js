// const cluster = require('cluster');
// const os = require('os');
//
// if (cluster.isMaster) {
//     for (let i = 0; i < (os.cpus().length / 2 + 1); i++) {
//         cluster.fork();
//     }
// } else {
//     require('./app');
// }

console.log('NodeJS《第二十七课: performance http xhr 网络请求性能优化的 process 多进程优化-cluster 多子进程集群优化》部分课程已完美的回溯完成!如需要检测执行结果,将以上代码注释解开即可!');
